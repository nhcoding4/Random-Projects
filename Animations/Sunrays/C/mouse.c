#include "def.h"

// --------------------------------------------------------------------------------------------------------------------

void updateMouse(Mouse *mouse)
{
    mouse->x = GetMouseX();
    mouse->y = GetMouseY();
    Vector2 mouseVec = {mouse->x, mouse->y};
    mouse->mouseVector2 = mouseVec;
}

// --------------------------------------------------------------------------------------------------------------------
